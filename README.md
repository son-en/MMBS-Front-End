
  # Makati Milk Bank System Prototype

  This is a code bundle for Makati Milk Bank System Prototype. The original project is available at https://www.figma.com/design/VllRMesk9eTSQToIoqaKqH/Makati-Milk-Bank-System-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  